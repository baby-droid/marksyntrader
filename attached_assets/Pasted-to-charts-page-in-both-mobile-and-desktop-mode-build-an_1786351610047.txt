to charts page in both mobile and desktop mode build and fix this , in the tick scanning logic is like this if a user switches to a different market it scans again 50 ticks all ticks then after the count reaches 50 it checks the best market to trade according to the 50 ticks that were analysed and after 2 mins it should analyse fresh 50 ticks and checks the market that mets the tick data that has a high win rate , then the AI sets the market it is trading in the last prediction digit the it looks for entry , it uses the entry point logic then when met it executes trade and it counts the wins and losses ,,,,, same loop accurate and fast also the AI accesses the digit distribution circle card to check digit % to check which market has met condition and it start to trade and scan ticks , the AI edits ticks , stake , prediction digit and executes trades , for 1s markets the AI can trade with markets conditions 10.6% and below not just below % add this also in jump markets and for bear and bull markets , plain index volatility make it below 10.2 % not just 10 % so that we can get markets to trade , after the AI has collected 50 ticks and then a market has met codition to trade under 7 it should now start looking for entry point and executing it should display analysing entry point e.g 2 then confirming 2 , 1 tick or 3 ticks , then waiting to execute it should show the user and it should trade continues checking for entry and if on a loss it should look for a fresh entry each entry max of 3 to 4 trades , the also add opposite recovery of over 4 or under 5 , the user should choose one in the opposite recovery on and off button and the AI should be sharp to look for and entry and recover using over or under 5 , then resume on profit to the default trade , when a user selects a market manualy e.g under 6 the AI should tell the user best entry to use and ticks ,,if full margin is on it starts with stake set by user and on loss it returns to 0.35 all the way up if it reaches 2 usd it returns to 0.35 + 0.1 every time it pushes 0.35 to 2 usd with 0 loss it plus 0.1 to show next cycle the AI scanner should not collide with moving triangle in digit distribution card it should watch will counting not making it stop ,when a user select over on it should trade over , same to under ,then add a strategy drop down list of the strategy I said you add reversal , tick concept , entry loop , conservative , number of losses , and all the others if a user selects any 3 or 4 strategies is the one the AI should use to trade , the AI should enter 3 trades then cool off for like 10 ticks then start trading again for 3 trades only , the entry point should also be observed keenly ,,if full margin is on it starts with stake set by user and on loss it returns to 0.35 all the way up if it reaches 2 usd it returns to 0.35 + 0.1 every time it pushes 0.35 to 2 usd with 0 loss it plus 0.1 to show next cycle the AI scanner should not collide with moving triangle in digit distribution card it should watch will counting not making it stop code all this changes well and finish well clean good job