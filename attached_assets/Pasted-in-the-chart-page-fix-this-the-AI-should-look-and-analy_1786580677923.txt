in the chart page fix this the AI should look and analyse like this in 1s and jump index markets the tick entry analysing should work like this if entry is 7 then the last digit flows like this 0,9,2,3,8,1 so this means skip zero then count 9 as first tick all the way to 1 as the 5th tick then using the current logic 1 tick and 4 tick have strong wins if I was trading over 2 market , then it should confirm the best ticks to use with 7 as entry the next flow of last digit , 4,2,6,0,9,3 so skip 4 then 1st tick at 2 and 3 as fifth tick this means that 4 ticks will be best to use since in analyses it won with 8 and in confirmation it won with 9 so use 7 entry with 4 ticks , then it should wait 10 to 15 ticks to anayse and confirm the best ticks in an entry point , after confirmation if the entry does not appear in 6 to 10 ticks it trades the opposite reverse trade , on loss it should trade the recovery set trade type , using digit cirlces distribution the side that has numbers in a row that are below 10.5 or 10.6 % it should trade either over or under , e.g if 0 and 1 are below 10.6 % it trades over 1 if 9 ,8and 7 are below 10.6 % it buys under 7 , and in bear and bull market , and plain index volatility the tick entry analysing should work like this if entry is 7 then the last digit flows like this 0,9,2,3,8,so this means count 0 as first tick all the way to 8 as the 5th tick then using the current logic the 2nd tick have strong wins if I was trading over 2 market , then it should confirm the best ticks to use with 7 as entry the next flow of last digit , 4,2,6,0,9, so then 1st tick at 4 and 9 as fifth tick this means that 5 ticks will be best to use since in analyses it won with 8 and in confirmation it won with 9 so use 7 entry with 5 ticks , then it should wait 10 to 15 ticks to anayse and confirm the best ticks in an entry point , after confirmation if the entry does not appear in 6 to 10 ticks it trades the opposite reverse trade , on loss it should trade the recovery set trade type , like in the picture the market should can trade over 0 to over 3 , so the AI can trade over 2 and 3 using best side that has a high chance of winning from the first 50 ticks count , so the AI also should trade the market set by user if user sets only over button on and sets in last digit prediction to 2 it means that the user wants the AI to trade over 2 and also same logic idea to under