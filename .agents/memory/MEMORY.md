- [Marksyntrader tab order](marksyntrader-tabs.md) — DBOT_TABS: DASHBOARD=0, AHMED_LEARNING=1 (Bot Builder tab), FREE_BOTS=2, DCIRCLES=3, SPEEDLAB=4, HEDGE=5, CHART=6, MANUAL_TRADER=7, AUTO_TRADES=8, COPY_TRADING=9, REPORT=10, BULK_TRADE=11, TUTORIAL=12, BOT_BUILDER=99 (never a visible tab).
- [Initialization loading screen](marksyntrader-loading.md) — Futuristic LoadingScreen (src/components/loading-screen/) replaces ChunkLoader in app-content.jsx; shows AHMED SYN TRADER branding + animated progress.
- [Merge conflict pattern](marksyntrader-conflicts.md) — bot-contents.ts and header/* files re-conflict after every task agent merge; resolve with python3 re.sub keeping HEAD section; verify with grep "^<<<<<<< " (7 chars), not "=======" which appears in legit comment dividers.
- [BotBuilder architecture](marksyntrader-botbuilder.md) — Single BotBuilder instance only in app-content.jsx (line 203); bot-builder tab content must be transparent (no background); BotBuilder uses position:fixed top:9rem z-index:10 when active_tab===1.
- [Currency display pattern](marksyntrader-currency.md) — All P/L/profit/stake values must use fromUsd()+displayCur from src/utils/currency-display.ts; subscribeCurrency() for live KSH/USD switch; never hardcode currency string.
- [Speed Lab / execution speed modes](marksyntrader-speedlab.md) — Normal=await settlement, Crazy=no-await+small in-flight cap, Turbo=zero-delay fire-and-forget no cap; apply same 3-tier pattern to every auto-trade loop (Speed Lab, AI Assistant, etc.), not just Speed Lab.
- [Run panel float-above-everything](marksyntrader-runpanel-float.md) — main.tsx renders both FloatingRunButton (correct) and an older inline RunStrategy panel; the inline one needs position:fixed + high z-index or it silently reverts to scrolling/low-z-index and collides with headers.
- [Copy-trading app id](marksyntrader-copytrading.md) — follower WS connections must authorize under the same NEXT_PUBLIC_DERIV_APP_ID as the main connection, not a generic/demo app id, or linking and reciprocation become unreliable.
- [Copy-trading source detection bug](copy-trading-source-bug.md) — account_info never had is_virtual; use getMasterSource() from trade-bus.ts (reads localStorage account_type) everywhere; never use api_base.account_info.is_virtual for source detection.
- [Copy trading global boot](copy-trading-global-boot.md) — copyEngine imported in app-content.jsx (global init + bridge); restoreState() called on login; chart buy function publishes master trades; bridge only active after copy-trading.ts first imports.
- [Chart page architecture](chart-page-architecture.md) — chart-wrapper.tsx owns tick subscription + digit state; DigitStatsOverlay (cdo classes) absolute-positioned at bottom of chart div; ChartTradePanel receives barrier+digit as props; no duplicate market selector; payout fetched via proposal API with 600ms debounce.
- [Free bots card size](free-bots-card-size.md) — cards reduced to ~50% size (padding 0.85rem, gap 0.55rem); Load Bot button is green (--green modifier); Load & Run uses --run modifier with accent gradient; btn-row is flex row with gap 0.4rem.
- [Digit widget pip_size bug](digit-widget-pipsize.md) — history batch arrives before live tick; must store raw prices and wait for authoritative pip_size from first live tick before computing digits, or wrong static defaults cause digit-0 at 99%+.
- [Dashboard STRATEGY.INIT navigation](marksyntrader-dashboard-init.md) — INIT now triggers handleOpen (load + Bot Builder nav); auto-select timer sets is_div_triggered_once before click so it also navigates — intentional "auto-open first bot" behavior, not a regression.
- [Draggable button pattern](marksyntrader-draggable-buttons.md) — use e.currentTarget (not e.target) for pointer capture/release; persist position with localStorage; suppress post-drag click via addEventListener once+capture; default bottom-left for risk disclaimer, middle-left for Free Bots.
- [ChunkLoadError recovery](marksyntrader-chunk-error.md) — quill-icons Illustration chunk fails after server restart; fixed by: (1) lazyCompilation:false in rsbuild dev+rspack experiments, (2) ChunkErrorPage errorElement on all React Router routes with sessionStorage one-shot reload guard.
- [Header demo/real buttons](marksyntrader-header-buttons.md) — demo account: left-side "Reset Demo" button (useResetDemoBalance shared hook); real account: right-side green "Deposit" button (useDepositReal hook → cashier API → fallback to app.deriv.com/cashier/deposit).
- [Scalper Bots architecture](marksyntrader-scalper-bots.md) — built-in scalper strategies are Blockly XML files loaded into the real Bot Builder (like Free Bots), not a custom trading engine.
- [BOT_BUILDER tab constant bug](marksyntrader-bot-builder-tab-bug.md) — DBOT_TABS.BOT_BUILDER must equal AHMED_LEARNING's index or every caller (dashboard cards, tours, announcements) blanks the screen.
- [Unified Settings panel](marksyntrader-settings-unified.md) — one settings surface (SettingsPanel) opened via ui_store.is_settings_panel_open from every entry point; SettingsModal was removed as a duplicate.
- [Scalper bots terminal entry logic](scalper-terminal-entry.md) — tick-based entry detection; `checkEntry()` uses contrarian streaks; `subscribeMarket()` updates both ref (for loop) and state (for display).
- [Loading screen two-phase](loading-screen-phases.md) — phase 1 (0-50%) = landing page; phase 2 (50-100%) = AHMED TRADE centered screen; switch via `useEffect` on progress >= 50.
- [Numeric input clamp-on-keystroke bug](marksyntrader-number-clamp-bug.md) — recurring bug pattern; shared fix is `src/components/number-field`; check every page's numeric inputs for it, not just the one reported.
- [Scalper XML single-engine design](scalper-xml-single-engine.md) — terminal only detects entry signals now; the real Bot Builder XML is the sole buyer via onRunButtonClick, guarded by globalObserver('bot.contract'/'bot.stop').
- [Scalper algorithm system](scalper-algorithms.md) — 5 algorithms (LDP, Market%, Sequence Radar, Complex Patterns, Entry Point Pattern) all share StrategyCondition type; use COND_DEFAULTS spread + algorithm-specific fields with optional chaining (?? default).
- [Scalper tick-driven scan](scalper-tick-scan.md) — tickSignalRef wakes the scan loop on every live tick (subscribeMarket fires it); 2s fallback timeout keeps it from hanging on slow ticks; never use fixed setInterval/setTimeout polling in the entry scan loop.
- [Scalper startBot logic](scalper-startbot-logic.md) — martingale carries across cycles via computeNextStake; stop after every win; await XML reload; force-resub feed on Run; switchOnLosses drives market switch.
- [Rise/Fall scalper trade type](scalper-risefail-tradetype.md) — rise-scalper.xml and fall-scalper.xml must use TRADETYPECAT_LIST=callput (not digits); checkEntry for CALL/PUT uses price momentum (≥3 consecutive falls/rises via priceWindowRef); priceWindowRef tracks raw prices alongside digitWindowRef.
- [Scalper RM overrideStake ceiling](scalper-rm-ceiling.md) — Risk Manager overrideStake is a ceiling (like Trade Params stakeOverride): RM martingale from base*multiplier^n; if ≥overrideStake, reset to base; caps std martingale below activateLimit too when inject=ON.
- [AI assistant Bot Builder mode](ai-assistant-bot-builder.md) — loadAndRun now loads XML into workspace + navigates to Bot Builder tab + calls run_panel.onRunButtonClick; monitors via globalObserver('bot.contract'/'bot.stop'); Fire Now stops after N successful WINS (not N total trades).
- [WhatsApp Float merged component](whatsapp-float-component.md) — WhatsAppFloat.tsx at src/components/floating/ replaces both WhatsAppButton and WhatsAppSignals; default top-left pos (x:20,y:100) draggable, localStorage SK_POS; CallMeBot signal sending via api.callmebot.com; listens to 'wa:signal' CustomEvent from scalper.
- [Scalper NDP + VPS + multi-contract features](scalper-advanced-features.md) — checkNDP() in index.tsx: 2-phase LDP+NDP reversal per contract type; VpsMode.tsx: auto-restart, TP/SL/runs limits, health monitoring; multi-contract: cfg.multiTradeCount sequential cycles per entry for multiple bots; patchXmlContent() patches market+duration in XML string before load; all market switches call onPreloadXml(bot,{market,duration}).
- [NDP moved into Algorithm dropdown + VPS restart fix](scalper-ndp-algorithm-vps-restart.md) — NDP is now a per-condition Algorithm option, not a global accordion; VPS restart was silently broken by a stale `.sb-run-btn` DOM selector, fixed to call `startBot()` directly.
- [Scalper flat-stake race](scalper-stake-flat-bug.md) — 'bot.stop' fires mid-teardown before DBot resets its interpreter; next run can race it, freezing stake. Fixed by polling `api_base.is_stopping` in runXmlBotCycle before firing.
- [Scalper TP/SL, deactivate limit, markets](scalper-tpsl-deactivate-markets.md) — tpGuard always true; loss_limit breaks outer loop (VPS stops); deactivate checks wins OR losses; symbolToSubmarket() helper; ALL_MARKETS includes Bear/Bull/Boom/Crash/Step/Range.
- [Hedge independent martingale per leg](hedge-martingale-per-leg.md) — runningStakeA/BRef track per-leg current stake; win resets to base, loss multiplies only that leg's stake independently.
